let Valor = prompt("Digite um valor: "); // Variavel para armazenar o valor que o usuario digitou

if (isNaN(Valor)) { // Verifica se o valor digitado é um número
  alert("Valor inválido!");
}

if (Valor > 0) { // Verifica se o valor é maior que 0
  alert("Valor positivo!");
}

if (Valor < 0) {  // Verifica se o valor é menor que 0
    alert("Valor Negativo!");
  }
  